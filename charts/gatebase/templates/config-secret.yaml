apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gatebase.fullname" . }}-config
  labels:
    {{- include "gatebase.labels" . | nindent 4 }}
type: Opaque
stringData:
  gatebase.yaml: |
{{ toYaml .Values.config | indent 4 }}
